# Transcoder service configuration
PORT=8323
API_KEY=
DB_PATH=./data/transcoder.db
CORS_ORIGINS=http://localhost:3001,https://homebot.kanakjr.in

# Paths to CLI tools (auto-detected from PATH if not set)
HANDBRAKE_CLI=
FFPROBE_PATH=
